#include "lexicon.hpp"
#include "iostream"
#include <fstream>
#include <cstring>

using namespace std;

void ASTToST(){
    if(!trees.empty()){
        Node *rootNode = trees.top();
        
    }
}

void recursiveStandardizer(Node root)